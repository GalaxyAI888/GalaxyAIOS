apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-nginx
  namespace: aistack-public
  labels:
    app: demo-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-nginx
  template:
    metadata:
      labels:
        app: demo-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.25-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: web-root
              mountPath: /usr/share/nginx/html
      volumes:
        - name: web-root
          hostPath:
            path: /run/desktop/mnt/host/d/data/nginx-html  # 对应 Windows 的 D:\data\nginx-html
            type: DirectoryOrCreate